/**
 * Design image Tool View plugin, node half.
 * The model-facing tools live on the product-safe preset. This row only
 * occupies official slots.
 */

/** Host plugin body — no host-side behavior for this presentation plugin. */
export function apply(): void {}
